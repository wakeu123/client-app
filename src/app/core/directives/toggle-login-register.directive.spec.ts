import { ToggleLoginRegisterDirective } from './toggle-login-register.directive';

describe('ToggleLoginRegisterDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleLoginRegisterDirective();
    expect(directive).toBeTruthy();
  });
});
